name: Build TRIDENT

on:
  push:
    branches: [ legacy ]
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            python: '3.10'
            name: windows
          - os: macos-latest  
            python: '3.10'
            name: macos
          - os: ubuntu-latest
            python: '3.10' 
            name: linux

    runs-on: ${{ matrix.os }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install numpy pybind11

    - name: Build
      run: |
        cd cpp
        mkdir build
        cd build
        cmake ..
        cmake --build . --config Release
    
    - name: Debug - List output files (Windows)
      if: runner.os == 'Windows'
      run: |
        echo "Contents of trident_add-on:"
        dir trident_add-on\ 2>nul || echo "trident_add-on directory not found"
        echo "Contents of cpp directory:"
        dir cpp\
        echo "Contents of build directory:"
        dir cpp\build\ 2>nul || echo "build directory not found"
        
    - name: Debug - List output files (Unix)
      if: runner.os != 'Windows'
      run: |
        echo "Contents of trident_add-on:"
        ls -la trident_add-on/ || echo "trident_add-on directory not found"
        echo "Contents of cpp directory:"
        ls -la cpp/
        echo "Contents of build directory:"
        ls -la cpp/build/ || echo "build directory not found"
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: trident-${{ matrix.name }}
        path: trident_add-on/trident*